---
title: Navigate agents
sidebarTitle: Navigate agents
description: How the agent switcher moves between the agent list and a single agent, what the agent Overview shows, and where agent creation starts.
icon: "compass"
---

import AgentBetaNote from '/snippets/python/langsmith/agent-beta-note.mdx';

LangSmith organizes a workspace around its [agents](/langsmith/agents), and one control in the top bar moves between them. This page covers that control, the two views it moves between, the addresses behind those views, and what an agent's Overview shows.

<AgentBetaNote />

## Switch between agents

The agent switcher sits at the left of the top bar, after the workspace switcher and the panel collapse toggle. It reads either **All agents** or the name of the agent you are in. In a section other than the Overview, a breadcrumb follows it and names that section.

The address behind it is `/o/<workspaceId>/a/<agentIdentifier>/...`. The workspace slot takes a UUID, and the agent slot takes the agent's identifier. A hyphen in the agent slot means all agents, so `/a/-` is the agent list and `/a/<agentIdentifier>` is a single agent. Either state can be set from the address bar, which is how to link directly to one of the two views.

{/*
  DOC-1597. The agent slot was written as `<agentId>`, which reads as the UUID. Two
  screenshots dated 2026-09-20 show otherwise. One address ends `/a/evaluators`. The
  other ends `/a/fleet-cf477f08ae7344d0b901c28f5f5c9290`, for an agent whose display
  name is "victor's slack agent" and whose ID field holds that same value. The
  workspace slot is a UUID and the agent slot is the identifier.

  That settled three claims which said otherwise, all now corrected: the switcher and
  the ID field further down this page, the address bar clause in log-traces-to-agent,
  and the third-value paragraph in agents.mdx. No agent UUID appears anywhere in the
  UI.

  The breadcrumb can carry more than one segment. Engine's setup screen reads
  "Engine / Set up" after the agent. The sentence above says only that it names the
  section, which stays true. Background in DOC-1597.
*/}

## The two views

The switcher moves between two views:

- **All agents**: The agents in the workspace, one card each.
- **A single agent**: That agent's Overview.

The left navigation lists the same items in both views, but two things differ. Its badge counts appear on the agent list, where they count the whole workspace, and are not shown once you select an agent, so a count beside a navigation item never describes the agent you are in. And several sections cannot be opened without an agent.

## Sections that require an agent

**All agents** is a valid state for the Overview and for Deployments, which lists every deployment in the workspace. Tracing, Monitoring, Engine, Build, and Studio each need a specific agent, and what you get when you open one with **All agents** selected depends on whether the workspace has any agents yet.

With agents to choose from, a **Pick an agent to view** screen replaces the section, below the top bar, so the switcher stays available. It holds a searchable list, and what sits under the list depends on the section: **+ New agent** on Tracing and Build, **Connect Agent Server** on Studio, and nothing on Monitoring or Engine. Build also offers a different set of agents from the other sections.

With no agents in the workspace, Build and Studio show that same screen reading **No agents available**, while Tracing, Monitoring, Engine, and Deployments show a get-started screen for the section instead of asking you to pick.

Once you pick an agent, LangSmith remembers it, so moving between these sections does not ask again, and the switcher stops offering **All agents**.

{/*
  DOC-1597. The two-state wording above is deliberate, and earlier claims were
  corrected against a walk of every sidebar item:

  1. The picker screen was described as having no top bar and therefore no switcher.
     It has both. Screenshots of all six sections dated 2026-09-20 show the top bar
     with the switcher reading "All agents" and a breadcrumb naming the section. The 2026-09-17 demo deck shows that screen with
     nothing above it and is an older build. This clause was once cut on the strength
     of that deck and had to be restored. Do not cut it again from the same source.
  2. The action below the list was described as belonging to Build and Studio alone.
     All six sections were captured on 2026-09-20. Tracing and Build show
     "+ New agent", Studio shows "Connect Agent Server", Monitoring and Engine show
     nothing, and Deployments has no picker. Build's "+ New agent" came from the
     original walk and the walk was right.
  3. Deployments was listed among the sections that require an agent. It is not one.
     With "All agents" selected it shows every deployment in the workspace, 20 rows
     matching its badge count, with its own "+ Deployment" and "LangSmith Studio"
     actions and no picker.
  4. Build was described as asking with its own control, then as showing the same
     screen as Studio with only the button differing. Three things differ. Build and
     Studio both brand the screen with a section header, "Build" and "LangSmith
     Studio", over "Pick an agent to view" as a subtitle, while Tracing, Monitoring,
     and Engine use the plain heading. The button differs. And Build's agent list is
     a different set: of the eight rows visible on 2026-09-20 it shared only the two
     Fleet-created agents with the other sections, and the rest were MDA-flavored,
     with no SDK-traced agent among them. That looks like Build filtering to the
     agents it can open, but the lists scroll and the rule is unverified, so the
     prose says only that the set differs.
  5. The picker was described as the uniform behavior for every agent-required section.
     Only Build and Studio showed it in an empty workspace.

  The header split is left out of the prose because it does not change what the
  reader does.

  The empty-workspace half was checked in a production workspace and the populated half
  in dev. If a production workspace with agents behaves differently, this splits again.

  The badge-count sentence above was checked against screenshots dated 2026-09-20 and
  stands as written. In the current build the counts are workspace-wide on the agent
  list and absent once an agent is selected. The proposed build, review app
  pr-41160-fe, shows them in both states and rescopes them to the selected agent, so if
  that build ships, this sentence inverts and the warning at the end of it has to go.

  Background is in DOC-1597.
*/}

## Settings replaces the shell

Opening Settings replaces the shell: the top bar and the agent switcher are gone, the navigation splits into Organizations and Workspace sections, and a link returns to the agent views, reading **Back to LangSmith** or, from Fleet, **Back to Fleet**.

## Read the agent Overview

Selecting a single agent lands on its Overview, which shows the following, in order:

- **Metrics**: Traces over time as a line chart, then trace count, error rate, token count, and total cost, each over the period named above them.
- **Tracing**: One row per environment the agent has, including environments with no traces, with the most recent run, trace count, error rate, P50 latency, P99 latency, and description.
- **Engine**: How many active issues [Engine](/langsmith/engine-overview) has found across this agent's environments, and a link into Engine. With no issues found, the section describes Engine rather than listing them.
- **Evaluate your agent**: A pointer into [evaluators](/langsmith/evaluators).
- **Monitor your agent**: A pointer into [dashboards](/langsmith/dashboards).
- **Deployments**: The [deployments](/langsmith/deployment) bound to this agent, and a link to all of them. With none, the section describes deployment rather than listing them, as the Engine section does. It does not appear at all for an agent [built in the UI](/langsmith/build-an-agent), which runs on a Studio runtime and cannot have one.

Everything on this screen is scoped to the agent you have selected. A line above the metrics states the window and the scope they cover, reading **Showing data from the last 7 days across all environments**, so the metrics include local and staging runs rather than production alone. The tracing section below them breaks the same period out by environment.

{/*
  DOC-1597. Settled: the Overview numbers are inclusive of all environments. The top
  number cards are additive across environments, the traces table breaks out by
  environment, and the top-level environment switcher has been removed.

  This resolves a conflict rather than confirming a guess. An earlier launch note
  recorded the Overview as Production-only, and it was stale.

  The tracing table lists every environment the agent has, not only the ones with
  traffic. Captured 2026-09-20 on agent-demo15, an agent with four environments and
  zero traces: four rows, each reading 0 traces and em-dashes. The section's own
  subtitle in the product says "Every environment this agent traces to", which is
  wrong on that screen and is not the wording to copy.

  That table runs least production-like first, Local then Development then Staging
  then Production. The environment dropdown runs the other way, most production-like
  first, which is what agent-environments.mdx documents. Both orders are correct for
  their own control. Do not reconcile them.

  The Deployments bullet said the section appears only for an agent that has
  deployments. It does not. agent-demo15, captured 2026-09-20 with no deployments,
  shows the section describing deployment behind a "Deploy an agent" link, the same
  shape Engine uses when it finds no issues.

  The absence is real but the rule behind it is inferred from two agents. The section
  was missing on victor's slack agent, a "fleet-" agent, and present on agent-demo15,
  which is not one. Both have zero deployments, so presence does not track deployments.
  Source type is the likely driver and matches what build-an-agent.mdx says about the
  Studio runtime. If a non-Fleet agent ever turns up without the section, the rule is
  something else. Background is in DOC-1597.
*/}

Two names for the same agent appear on this screen, and they are not interchangeable. The heading and the switcher both show the display name. A copyable field labeled **ID** holds the identifier, which is also the value the address bar carries.

Use the copy button beside **ID** when you need the value for tracing. [Agent addressing](/langsmith/log-traces-to-agent) takes the identifier, and reconstructing it from the display name does not work, because the two can differ. For which value is which, see [Identifiers and display names](/langsmith/agents#identifiers-and-display-names).

## Create an agent

Three controls open the **New Agent** panel: **+ Agent** above the agent list, and **+ Create new agent** and **Trace existing agent** on a creation card at the end of the list, also titled **New Agent**. The first two open the panel on its grid of options, while **Trace existing agent** opens it on the Observability option. Choosing an option replaces the grid with a guided setup flow, and an **All options** link returns to the grid. The four options map to four different parts of this documentation:

- **Builder**, "Build with AI inside LangSmith". This opens the **Build** tab, which creates a Managed Deep Agent from a name and a description instead of from code. See [Build an agent in the UI](/langsmith/build-an-agent).
- **Managed Deep Agent**, "Build locally and deploy". See [Managed Deep Agents](/langsmith/python/managed-deep-agents-overview). This route requires a paid plan. Whether the panel marks it with a **Plus** badge depends on your plan.
- **Open Source**, "Build an agent with our open source frameworks". See [LangChain](/oss/python/langchain/overview).
- **Observability**, "Trace an existing agent". See the [tracing quickstart](/langsmith/observability-quickstart), or [LangSmith skills](/langsmith/skills) for the prompt-driven setup the panel offers first.

Creating an agent for an application that is traced from outside LangSmith uses a separate control. The agent switcher's **New agent** action opens a **Create a new agent** dialog, which takes an agent name and creates the agent from it. That action requires permission to create projects, and it appears on the tracing project pages, not on the agent views.

{/*
  DOC-1597. Four controls create an agent and no two of them share a label:

  - "+ Agent", above the agent list, opens the "New Agent" panel. Confirmed in the
    current build and the proposed one on 2026-09-20.
  - "+ Create new agent" and "Trace existing agent", on the "New Agent" card at the end
    of the agent list. Confirmed 2026-09-20. "+ Create new agent" opens the panel on
    its option grid, and "Trace existing agent" opens it on the Observability option.
  - "+ New agent", below the "Pick an agent to view" screen on Tracing and Build.
    Confirmed 2026-09-20. It also appears in the 2026-09-17 deck, which is an older
    build and is not what this rests on.
  - "New agent", the agent switcher's action, opening a "Create a new agent" dialog.
    From the original UI walk, and not in any screenshot.

  Do not normalize these into one spelling. An earlier draft gave the card's control as
  "Build new Agent", which does not exist.

  The Plus badge on the Managed Deep Agent option did not render in the workspace
  captured on 2026-09-20, which is an internal one on a paid plan, so the badge is
  plan-dependent. The rule behind it is unverified, which is why the page says only
  that it depends on your plan.

  The Observability option's flow, captured 2026-09-20: generate an API key, then
  either paste a prompt into a coding agent or set up manually, then a completion step
  reading "Traces detected". The prompt installs the skills from
  github.com/langchain-ai/langsmith-skills and runs the langsmith-trace skill, which is
  why that bullet now also links /langsmith/skills. The other three options' flows were
  not captured, so only this one is described. Background is in DOC-1597.
*/}

## See also

- [Agents](/langsmith/agents)
- [Agent environments](/langsmith/agent-environments)
- [Tracing quickstart](/langsmith/observability-quickstart)

---

<div className="source-links">
<Callout icon="terminal-2">
    [Connect these docs](/use-these-docs) to Claude, VSCode, and more via MCP for real-time answers.
</Callout>
<Callout icon="edit">
    [Edit this page on GitHub](https://github.com/langchain-ai/docs/edit/main/src/langsmith/navigate-agents.mdx) or [file an issue](https://github.com/langchain-ai/docs/issues/new/choose).
</Callout>
</div>
