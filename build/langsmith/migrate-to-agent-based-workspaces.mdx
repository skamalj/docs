---
title: How agent-based workspaces differ
sidebarTitle: Workspace migration
description: What changes when a workspace moves from organizing traces by tracing project to organizing them by agent and environment.
icon: "arrow-right"
tag: "Beta"
---

LangSmith organizes a workspace in one of two ways. A _project-based workspace_ groups traces by [tracing project](/langsmith/observability-concepts#tracing-projects). An _agent-based workspace_ groups them by [agent](/langsmith/agents), and divides each agent's traces across [environments](/langsmith/agent-environments) drawn from a fixed set of four.

Agent-based organization is in [beta](/langsmith/release-stages) for a private group of customers, so most workspaces are project-based. This page covers what is different in an agent-based workspace, and what changes if yours becomes one.

## Check which one you have

The control at the top left tells you. In an agent-based workspace it names your workspace and opens a list of the workspaces in your organization. In a project-based workspace it reads **LangSmith** and switches between LangSmith and Fleet, and the sidebar has an **Application** section with an application picker.

{/*
  DOC-1597. The test above was "Overview is the first item under Search" until
  2026-09-20. Do not restore it. The first sidebar item is state-dependent, not
  architecture-dependent: in the proposed build it reads "All agents" with no agent
  selected and "Overview" with one selected, so the test fails for any reader sitting
  on the agent list.

  The top-left control is the stable tell. It named the workspace in every agent-based
  state captured, closed and open, agent selected and not, in both the current build
  and the proposed one, and read "LangSmith" in the project-based workspace. The
  Application section stays as a second confirm for project-based, because it costs
  nothing and is what that snippet already said. That half rests on one production
  workspace, so if a deployment labels the control something other than "LangSmith",
  the Application section is the part to lean on.

  The agent picker was dropped as a tell, but not for the reason first recorded here.
  That reason was that the 2026-09-17 deck shows it absent from the picker screen, so
  it was not reliably visible. Dev captures dated 2026-09-20 show it present on all six
  sections, and the deck is an older build. The real reason is that it sends the reader
  to two different places, the top bar in one arrangement and the sidebar in the other,
  while the top-left control is one place in both.

  This sentence appears in four places, weighted for each context. See the not-applied
  finding in the PR for why they are not one shared snippet. Background in DOC-1597.
*/}

## What changes

The organizing container changes, and everything scoped to it follows:

- **Traces** group under an agent and one of its environments instead of under a tracing project you named. Nothing is re-ingested or moved: each environment keeps the tracing project that backed it, renamed to the agent identifier, a hyphen, and the environment. An agent whose identifier is `checkout` has its production traces in `checkout-production`.
- **Prebuilt dashboards** cover an environment rather than a named project. See [Monitor projects with dashboards](/langsmith/dashboards).
- **Alerts** are scoped to an environment of an agent. See [Alerts](/langsmith/alerts).
- **Evaluators** attach to an environment. See [Manage evaluators](/langsmith/evaluators).
- **Navigation** gains an agent picker in the top bar, which moves between the agent list and a single agent's Overview. See [Navigate agents](/langsmith/navigate-agents).
- **The top-left control** becomes a workspace switcher. In a project-based workspace it reads **LangSmith** and moves between LangSmith and Fleet. In an agent-based workspace it names your workspace, lists the other workspaces in your organization, and reaches Fleet through a **Go to Fleet** link at the end of that list.

What does not change: your traces, datasets, prompts, and experiments are the same records, and existing tracing configuration keeps working. See [Log traces to a specific project](/langsmith/log-traces-to-project).

## What the change is worth

Agents and environments record what a tracing project cannot: which application produced a trace, and which deployment of it. LangSmith can then act on that context rather than asking you to filter for it, so an alert can watch production and stay quiet about local runs, and a dashboard can compare one agent across environments without a saved filter per environment.

For the full comparison with tracing projects, see [Why an agent rather than a project with environments](/langsmith/agents#why-an-agent-rather-than-a-project-with-environments).

## How a workspace moves

LangChain enables agent-based organization per workspace for customers in the beta. There is no setting in the product for choosing between the two, and no action is required from you.

During the beta, the setting applies when a workspace is created, so a project-based workspace stays project-based. Existing workspaces are not switched automatically.

<Note>
This page describes the change, not its schedule. Timing is not published.
</Note>

## See also

- [Agents](/langsmith/agents)
- [Agent environments](/langsmith/agent-environments)
- [Workload isolation](/langsmith/workload-isolation)
- [Administration overview](/langsmith/administration-overview#agents-and-applications)

---

<div className="source-links">
<Callout icon="terminal-2">
    [Connect these docs](/use-these-docs) to Claude, VSCode, and more via MCP for real-time answers.
</Callout>
<Callout icon="edit">
    [Edit this page on GitHub](https://github.com/langchain-ai/docs/edit/main/src/langsmith/migrate-to-agent-based-workspaces.mdx) or [file an issue](https://github.com/langchain-ai/docs/issues/new/choose).
</Callout>
</div>
